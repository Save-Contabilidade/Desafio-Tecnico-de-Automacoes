# src

Implemente nesta pasta o código principal da sua solução.

A estrutura interna é livre.

Sugestão de ponto de entrada:

```text
src/main.py
```

Execução sugerida:

```bash
python -m src.main --input input/solicitacoes.csv --output output/resultado.csv
```
